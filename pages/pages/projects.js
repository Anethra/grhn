import Layout from '../../components/layout'
import Styles from '../../styles/home.module.css'

export default function Projects() {
    return (
        <Layout>
            <p className={Styles.center}>We no have anythink here yet.</p>
        </Layout>
    )
}